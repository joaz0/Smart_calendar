Componentes:
Sufixo obrigatório: Component

PascalCase: DayViewComponent, MonthViewComponent

Seletor: prefixo app- + kebab-case: app-day-view

Interfaces/Types:
PascalCase: CalendarEvent, UserSettings

Prefixos de interface: I opcional (não recomendado)

Métodos/Variáveis:
camelCase: getEventsForDate(), currentUser

Métodos públicos com verbo: loadData(), saveSettings()

Métodos privados com underscore: _privateMethod()
